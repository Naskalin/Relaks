using Relaks.Interfaces;

namespace Relaks.Mappers;

public static class MiscMapper
{
    // public static string ToFtsDeletedAt(this ISoftDeleted softDeleted) => softDeleted.DeletedAt != null ? "1" : "";
}