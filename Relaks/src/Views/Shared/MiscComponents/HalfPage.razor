<div class="row">
    <div class="col-6 @(IsCentered == true ? "offset-3" : "")">
        @ChildContent
    </div>
</div>

@code {
    [Parameter]
    public RenderFragment ChildContent { get; set; } = null!;
    
    [Parameter]
    public bool? IsCentered { get; set; }
}